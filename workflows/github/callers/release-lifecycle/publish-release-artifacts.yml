name: Publish Release Artifacts

run-name: Release ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: Existing immutable v<semver> tag to release.
        required: true
        type: string

permissions: {}

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Release delivery
    permissions:
      contents: write
      id-token: write
      attestations: write
    uses: t33n-software/git-governance/.github/workflows/reusable-publish-release-artifacts.yml@4573d36bbf928221e6d38e151e68aec6c168bb04
    with:
      tag: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
      delivery_variant: ${{ vars.GIT_GOVERNANCE_DELIVERY_VARIANT }}
      delivery_environment: release-delivery
      repository: ${{ github.repository }}
      source_gate: ${{ vars.GIT_GOVERNANCE_SOURCE_GATE }}
