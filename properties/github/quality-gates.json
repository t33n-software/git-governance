{
  "value_type": "single_select",
  "required": true,
  "default_value": "pending",
  "description": "Quality-gate class partition for the organization ruleset selectors. full: repositories shipping per-OS binaries; linux-only: repositories shipping Linux/Docker artifacts only; pending: workflows not yet aligned with a class.",
  "allowed_values": [
    "full",
    "linux-only",
    "pending"
  ],
  "values_editable_by": "org_actors"
}
